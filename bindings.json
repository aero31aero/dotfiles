{
	".tmux.conf": {
		"target": "~/.tmux.conf",
		"notes": "Clone the themes repository and change the path to it in the conf accordingly."
	},
    "rofi" : {
        "target": "~/.config",
        "notes": "Launch command for rofi:rofi -modi combi -combi-modi window,drun,run -show combi -theme ~/.config/rofi/sidebar.rasi -matching fuzzy"
    }
}
